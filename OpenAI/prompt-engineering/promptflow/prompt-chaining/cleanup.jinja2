Extract and return **only** the Python code blocks from the following text. Remove all text explanations, markdown formatting, or additional context. If there are multiple Python code blocks, concatenate them clearly separated by two newlines. If no Python code is found, return an empty response:

---

{{text}}

---

Output:

# extracted Python code here
